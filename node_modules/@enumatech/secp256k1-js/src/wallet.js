(function() {
    if (typeof module !== 'undefined') {
        Secp256k1 = module.require('./secp256k1')
        module.exports = exports
    }
    else {
        exports = Wallet = {}
    }

    exports.sign = function sign () {
        Secp256k1.decompressKey()
    }
})()
